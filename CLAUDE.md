# CLAUDE.md

## Project Notes

- Do not add file size limits on imports. All parsing is client-side in the browser, so there is no server resource concern. Let the browser handle arbitrarily large files.
